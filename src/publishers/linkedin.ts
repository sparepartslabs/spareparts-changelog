import type {ReleaseArtifact,LinkedInConfig} from "../domain/release.js"; import type {DestinationResult} from "../domain/result.js"; import type {Publisher} from "./publisher.js"; import {projectLinkedIn} from "./linkedin-content.js"; import {sanitize} from "../config/secrets.js";
export type Fetcher=typeof fetch;
export class LinkedInPublisher implements Publisher {constructor(private config:LinkedInConfig,private request:Fetcher=fetch){}async publish(a:ReleaseArtifact):Promise<DestinationResult>{try{const res=await this.request("https://api.linkedin.com/rest/posts",{method:"POST",headers:{Authorization:`Bearer ${this.config.accessToken}`,"LinkedIn-Version":"202501","X-Restli-Protocol-Version":"2.0.0","Content-Type":"application/json"},body:JSON.stringify({author:this.config.author,commentary:projectLinkedIn(a),visibility:"PUBLIC",distribution:{feedDistribution:"MAIN_FEED",targetEntities:[],thirdPartyDistributionChannels:[]},lifecycleState:"PUBLISHED",isReshareDisabledByAuthor:false})});if(!res.ok)throw new Error(`LinkedIn rejected request (${res.status})`);return {destination:"linkedin",status:"succeeded",location:res.headers.get("x-restli-id")??undefined,message:"Published"}}catch(e){return {destination:"linkedin",status:"failed",errorCode:String(e).includes("exceeds")?"content-limit":"remote",message:sanitize(e)}}}}
