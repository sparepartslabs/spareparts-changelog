import {S3Client,PutObjectCommand} from "@aws-sdk/client-s3"; import type {ReleaseArtifact,S3Config} from "../domain/release.js"; import type {DestinationResult} from "../domain/result.js"; import {sanitize} from "../config/secrets.js"; import type {Publisher} from "./publisher.js";
export interface S3Sender{send(command:PutObjectCommand):Promise<unknown>}
export class S3Publisher implements Publisher {constructor(private config:S3Config,private client:S3Sender=new S3Client({region:config.region,endpoint:config.endpoint,forcePathStyle:config.forcePathStyle})){} async publish(a:ReleaseArtifact):Promise<DestinationResult>{try{await this.client.send(new PutObjectCommand({Bucket:this.config.bucket!,Key:this.config.key!,Body:Buffer.from(a.markdown),ContentType:"text/markdown; charset=utf-8"}));return {destination:"s3",status:"succeeded",location:`s3://${this.config.bucket}/${this.config.key}`,message:"Published"}}catch(e){return {destination:"s3",status:"failed",errorCode:"remote",message:sanitize(e)}}}}
