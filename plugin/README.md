# Spare Parts Changelog Codex Plugin

The plugin guides local generation and GitHub Action configuration while delegating deterministic rendering to the `spareparts-changelog` CLI. Repository output is the default. S3 and LinkedIn require separate explicit consent and caller-managed secrets.

Validate with the plugin-creator validator before distribution.
