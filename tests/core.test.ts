import {describe,it,expect} from "vitest";import {render} from "../src/render/markdown.js";import {categorize} from "../src/conventional/categorizer.js";import {parseBoolean,validateRequest} from "../src/config/schema.js";import {sanitize,registerSecret} from "../src/config/secrets.js";import {projectLinkedIn} from "../src/publishers/linkedin-content.js";
describe("core",()=>{it("categorizes and renders deterministically",()=>{const commits=[{hash:"a",subject:"feat: Add sharing",body:"",position:0},{hash:"b",subject:"fix!: Stop data loss",body:"BREAKING CHANGE: changed",position:1},{hash:"c",subject:"chore: internal",body:"",position:2}];const c=categorize(commits);const a=render("v1",c.entries,c.omitted);const b=render("v1",c.entries,c.omitted);expect(a.markdown).toBe(b.markdown);expect(a.markdown).toContain("Breaking Changes");expect(a.markdown).toContain("Added");expect(c.omitted).toBe(1)});it("validates safe defaults",()=>{expect(parseBoolean("TRUE",false)).toBe(true);expect(()=>parseBoolean("yes",false)).toThrow();expect(()=>validateRequest({from:"a",to:"b",identity:"v1",changelogPath:"../x",writeRepository:true,s3:{enabled:false},linkedin:{enabled:false}})).toThrow()});it("redacts and rejects oversized LinkedIn",()=>{registerSecret("canary-token");expect(sanitize("Bearer canary-token")).not.toContain("canary-token");const a=render("v1",[],0);expect(()=>projectLinkedIn(a,2)).toThrow()})});
